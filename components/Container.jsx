import { View, Text } from 'react-native';
import React from 'react';

const Container = ({ children }) => {
    return <View className="pt-32 pr-4 pl-4 ">{children}</View>;
};

export default Container;
